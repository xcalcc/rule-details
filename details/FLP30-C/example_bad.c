void func(void)
{
    // x is a floating-point variable used for iteration
    for (float x = 0.1f; x <= 1.0f; x += 0.1f)
    {
        /* ... */
        // Loop can iterate 9 or 10 times depending on compiler
    }
}