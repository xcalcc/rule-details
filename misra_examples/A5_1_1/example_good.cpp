#include <stdio.h>
#include <stdint.h>

#include <stdio.h>
#include <stdint.h>

void function( void )
{
    const int max_length = 100;
    for (int i = 0; i < max_length; i++)
    {
        // compliant
    }
}