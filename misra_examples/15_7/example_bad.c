#include <stdbool.h>

void func() {
    bool flag;

    if (flag == true) {
        // Do something
    }
    else if (flag == false) {
        // Do something
    }
    // Non-compliant as there is no else statement
}